# 2026-03-20 日志

## ClawClau v2 完整重建（承接 2026-03-19 晚间工作）

### 进度汇报 bug 修复（22:12）
- **问题1**：完成后还推了一条进度（while 循环在 sleep 前检查 session，sleep 期间 session 关了但醒来还会检查日志发通知）
- **问题2**：snippet 为空时仍然发空消息 `""`
- **修复**：`while true` + sleep 后先 `tmux has-session || break`，snippet 空则 `continue`
- 修复位置：`claude-spawn.sh` 进度汇报循环（~231-260行）

### 最终验证任务（22:15-22:35）
- 任务 `clawclau-final-test`：让乌萨奇写 bats 测试套件
- 超时 1200s 被杀（20分钟），但乌萨奇完成了全部 8 个测试文件（58KB）
- 卡在最后跑 bats 执行验证那步
- 产出文件：`~/.openclaw/workspace/tests/` 下 7 个 .bats + test_helper.bash
- 验证结果：完成后不再多发重复进度（修复生效），但进度内容 `""` 仍然出现（乌萨奇大部分输出是 tool_use 不是文本，这是内容抓取局限不是逻辑 bug）

### 乌萨奇自动重构了自己的代码（22:01-22:10，任务 clawclau-slow-test）
- clawclau-lib.sh：PATH export 提到顶部、14个函数加 JSDoc 注释、cc_tmux_alive 标 deprecated、jq 预校验防注入
- claude-spawn.sh：--interval 注释修正、_wait_tmux_ready() 替代硬编码 sleep、主流程拆成 build_wrapper/start_session/launch_monitors 三个函数

### GitHub 仓库创建（23:03-23:08，任务 clawclau-github）
- 仓库地址：https://github.com/BarryYJJ/clawclau
- 内容：8个脚本 + SKILL.md + 8个测试文件 + README.md

### ClawHub 更新（23:05-23:08，任务 clawclau-clawhub-update）
- 版本：v2.0.0
- Skill ID：k97c24t73fyna3x5ya0k0dz8bn837hha
- SKILL.md 反映 v2 架构（双重 fork、飞书直发、notify_chat 配置等）

### 隐私安全审查（23:12-23:15，任务 clawclau-privacy-check）
- 结论：无高危/中危泄露
- 3处低危：cron 示例中 `/Users/yjj/` 路径（2处）、GitHub 用户名 BarryYJJ、.openclaw 目录结构
- 桨桨说"就这样吧"，未修复

### ClawClau 通知路由讨论
- `notify_chat` 在 `~/.clawclau/config` 里写死了乌萨奇群 ID
- 不管从哪个群触发任务，通知都推到乌萨奇群
- 桨桨问是否要改成推到触发任务的群（动态 CC_NOTIFY_CHAT），尚未确认

## ClawClau v2 最终文件清单

**脚本：** `~/.openclaw/workspace/scripts/` 下 8 个文件
**使用指南：** `~/.openclaw/workspace/skills/clawclau/SKILL.md`
**运行时数据：** `~/.clawclau/`（config + active-tasks.json + logs/ + prompts/）
**测试：** `~/.openclaw/workspace/tests/` 下 8 个文件
**备份/分发：** GitHub (BarryYJJ/clawclau) + ClawHub (clawclau v2.0.2)

---

## 开箱即用化 + Skill 规范化（01:00-02:00）

### GitHub 加 install.sh（任务 clawclau-setup-script）
- 乌萨奇写了 install.sh + uninstall.sh + 更新 README
- 但 push 前问了"你允许我推送吗"卡住（claude -p 无法交互确认）
- 小八手动 push 成功：`e92f90c..7bdf7aa main -> main`
- 教训：print 模式任务的 prompt 里要明确说"不需要确认，直接执行"

### ClawHub 打包脚本（任务 clawclau-clawhub-scripts）
- 7 个脚本复制到 `skills/clawclau/scripts/` 下
- 发布为 clawclau v2.0.1
- 顺手把 cron 示例 `/Users/yjj/` 改成 `~/`

### 本地 Skill 结构审计（任务 clawclau-structure-audit）
- 结论：功能完整但不算"打包好的 skill"
- 必须修：SKILL.md 无 YAML frontmatter（触发系统无法识别）
- 建议：SKILL.md 280 行太长，参考内容应拆到 references/；脚本存在两处有歧义

### Skill 规范化修复（任务 clawclau-skill-fix）
- 加了 YAML frontmatter（name + description）
- SKILL.md 从 281 行瘦身到 142 行（-49%）
- 拆分出 references/schema.md + references/design.md
- 两处脚本（workspace/scripts/ 和 skills/clawclau/scripts/）确认完全一致
- ClawHub 发布 v2.0.2
- GitHub 推送 commit b6094ae

### SOUL.md 加乌萨奇触发规则（~01:59）
- 在"延续"段落前加了"乌萨奇触发规则"
- 内容：当用户说"让乌萨奇做xxx"或"用Claude xxx"时，读取 clawclau skill，通过 claude-spawn.sh 派发
- 所有群 session 都能读到（SOUL.md 是全局加载的）

### 通知路由结论
- notify_chat 固定写死乌萨奇群，不动态切换（稳定性优先）
- 任何群触发任务，通知都去乌萨奇群

### BABA 财报摘要（任务 baba-earnings-summary）
- 3 分钟完成，纯文本输出
- 验证了 ClawClau 处理中文文本任务的效果

### 待办
- [ ] 重启 OpenClaw 刷新 skill 列表（加载新的 clawclau frontmatter）
- [ ] 更新 FRAMEWORK.md 第三节（ClawClau v2 委派方式）
